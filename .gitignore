# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (CNG: se generan con `expo prebuild`, no se commitean)
/android
/ios
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files — se ignora cualquier .env*, salvo la plantilla.
# El patrón es amplio a propósito: listarlos uno por uno dejó fuera .env.qa,
# que se habría commiteado con lo que tuviera dentro.
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo

app-example
